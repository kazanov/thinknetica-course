<% if @answer.errors.present? %>
  <% @answer.errors.full_messages.each do |message| %>
    $('#answer_body').before('<%= j message %>')
  <% end %>
<% else %>
  $('#answers ul').append('<%= j render @answer %>');
  $('#answer_body').val('');
<% end %>
