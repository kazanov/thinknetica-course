= form_for @question, remote: true, html: { id: "edit-question" } do |f|
  .question-errors
  %p
    = f.label :title, 'Title:'
    = f.text_field :title
  %p
    = f.label :body, 'Body:'
    = f.text_area :body
  = render 'attachments/form', f: f
  = f.submit 'Save'
